#!/usr/bin/env ruby

require 'bundler/setup'
require 'fortuneteller'
require 'benchmark'

trials = (ARGV.first || 100).to_i

puts "simulating N=#{trials}:"
puts Benchmark.measure {
  trials.times { FortuneTeller::Benchmark.run }
}
